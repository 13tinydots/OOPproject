
function generateHtml(data) {
    for(i=0; i<data.length; i++){
        if(data[i].getRole() == "Manager"{
            templateLit  += `
            some HTML about manager
            `
        })
    }
}

module.exports = generateHtml;